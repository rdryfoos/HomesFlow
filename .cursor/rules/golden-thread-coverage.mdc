---
description: Regenerate coverage matrix and Golden Thread canvas after traceability changes
globs: specs/001-mvp/tasks.md,HomeFlow.prd.md,specs/001-mvp/spec.md,ios/HomeFlow/**/*.swift,ios/HomeFlowTests/**/*.swift
alwaysApply: false
---

# Golden Thread coverage refresh

When you finish work that changes any traceability input:

- `tasks.md` (task status, new tasks, or `Traces:` IDs)
- PRD / spec IDs
- `@covers` annotations in Swift source
- AC-prefixed unit test names (`test_AC_*`)

Then before marking the task complete:

1. Run `bash scripts/check-traceability.sh --refresh` from the repo root.
   - Gate 2 check (must pass)
   - Regenerates `specs/001-mvp/coverage.md` (commit this with code changes)
   - Updates the local Cursor canvas **HomeFlow Golden Thread — Coverage** (outside git; optional but recommended)

2. If Gate 2 fails, fix gaps or add pending test tasks — do not leave untracked ACs.

3. Do **not** tie canvas refresh to Xcode build phases or CI; `coverage.md` is the shared artifact.

Canvas path (override with `GOLDEN_THREAD_CANVAS`):

`~/.cursor/projects/Users-rik-Developer-HomeFlow/canvases/golden-thread-coverage.canvas.tsx`
